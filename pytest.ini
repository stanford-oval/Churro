[pytest]
# Show stdout/stderr immediately (disable capture) and always display all warnings.
addopts = -s -W always

# Ensure all warnings are shown (redundant with -W always, but explicit here).
filterwarnings =
    always